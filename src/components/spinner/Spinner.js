import spinner from "../../style/spinner.gif";

const Spinner = () => {
    return (
            <img src={spinner} alt="spinner" style={{margin: "0 auto", display: "block", width: "150px"}}></img>
    )
}

export default Spinner;